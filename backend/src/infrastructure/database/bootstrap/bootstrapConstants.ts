/** Bootstrap metadata version — bump when bootstrap flow changes. */
export const BOOTSTRAP_VERSION = '1.0.0';

/** Seed catalog version — bump when initial menu/user catalog changes. */
export const SEED_VERSION = 'pos-v1';

/** Singleton document id in `system_bootstrap`. */
export const BOOTSTRAP_DOC_ID = 'system';

export const SYSTEM_BOOTSTRAP_COLLECTION = 'system_bootstrap';
